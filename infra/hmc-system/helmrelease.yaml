---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hmc
spec:
  chartRef:
    kind: OCIRepository
    name: hmc
  targetNamespace: hmc-system
  interval: 10m
  install:
    crds: Create
    remediation:
      retries: -1
  upgrade:
    crds: CreateReplace
